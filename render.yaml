services:
  - type: web
    name: clinical-opd-backend
    env: node
    plan: free
    buildCommand: npm install
    startCommand: node app/backend/server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 5000
      - key: FRONTEND_URL
        fromContext: frontend_url
